# Local server
PORT=3001
HOST=0.0.0.0

# Supabase (profile project, layer 02 — service_role key, never commit real values)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=

# Comma-separated list of allowed browser origins (CORS)
ALLOWED_ORIGINS=http://localhost:3000

# Raw API key for local testing (mint with `pnpm --filter @portfolio/api-profile create-api-key`)
PROFILE_API_KEY=
